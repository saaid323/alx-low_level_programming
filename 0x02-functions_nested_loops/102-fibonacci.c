#include <stdio.h>

/**
 * main - Entery point
 * Return: 0 Always
 */

int main(void)
{
	int i, a, b;

	for (i = 0; a <= 20365011074; i++)
	{
		printf("%d, ", a);
		b = a + b;
		a = b - a;
	}
	return (0);
}
