#include "main.h"
#include <stdlib.h>

/**
   * _abs - Computes the absolute value of an integer
   * @i: The integer to be computed
   * Return: the absolute value
   */

int _abs(int i)
{
	int x;

	x = abs(i);

	return (x);
}
