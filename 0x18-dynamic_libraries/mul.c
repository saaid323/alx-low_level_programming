#include "math.h"

/**
 * mul - multiplies two numbers
 * @a: first number
 * @b: second number
 * Return: multiplication of a and b
 */

int mul(int a, int b)
{
	int re;

	re = a * b;
	return (re);
}
